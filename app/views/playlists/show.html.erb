<p id="notice"><%= notice %></p>

<%= @playlist.name %>
<%= link_to 'Edit', edit_playlist_path(@playlist) %> |
<%= link_to 'Back', playlists_path %>

<table>
  <tr>
    <th>Songs</th>
    <th></th>
  </tr>

<% @songs.each do |song| %>
    <tr>
     <td><%= song.name %></td>
     <td><%= link_to 'Edit', edit_song_path(song) %></td>
    </tr>
  <% end %>
</table>

<div id='playlist'></div>
#_playlist.html.erb
<%= form_for @playlist, :remote => true  %>

<li id="playlist_<%= playlist.id"%>

<%= @playlist.name %>

<% @songs.each do |song| %>
    <ul>
     <li><%= song.name %></td>
    </ul>
  <% end %>
<%= text_field
