$(".song<%= j(@song.id.to_s) %>_in_list<%= j(@playlist.id.to_s) %>").hide();
if(<%= @this_song.id %> == <%= @song.id %>) { 
$("#song_pref").append("<%= j(render 'add_playlist_to_song', :song => @song, :playlist => @playlist) %>");
};
